<UserControl x:Class="proyectodeloschikis.ArbolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:proyectodeloschikis"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,10,0">
                <TextBlock Text="Árbol Binario" FontSize="24" Foreground="#C084FC" Margin="0,0,0,15" FontWeight="Bold"/>

                <TextBox x:Name="txtValor" Background="#1A1A2E" Foreground="White"
                         BorderBrush="#C084FC" Height="40" FontSize="20" VerticalContentAlignment="Center"/>

                <StackPanel Orientation="Horizontal" Margin="0,10">
                    <RadioButton x:Name="rbEnteros" Content="Enteros" Foreground="White" GroupName="TipoDato" IsChecked="True" Margin="0,0,15,0"/>
                    <RadioButton x:Name="rbCaracteres" Content="Caracteres" Foreground="White" GroupName="TipoDato"/>
                </StackPanel>

                <Label Content="Edición" Foreground="White" Margin="0,10,0,5"/>
                <WrapPanel>
                    <Button Content="Insertar" Style="{StaticResource MenuButton}" Click="Insertar_Click"/>
                    <Button Content="Eliminar" Style="{StaticResource MenuButton}" Click="Eliminar_Click"/>
                </WrapPanel>

                <Label Content="Búsqueda" Foreground="White" Margin="0,10,0,5"/>
                <WrapPanel>
                    <Button Content="Buscar" Style="{StaticResource MenuButton}" Click="Buscar_Click"/>
                    <Button Content="Nivel" Style="{StaticResource MenuButton}" Click="Nivel_Click"/>
                </WrapPanel>

                <Label Content="Propiedades" Foreground="White" Margin="0,10,0,5"/>
                <WrapPanel>
                    <Button Content="Altura" Style="{StaticResource MenuButton}" Click="Altura_Click"/>
                    <Button Content="Mínimo" Style="{StaticResource MenuButton}" Click="Minimo_Click"/>
                    <Button Content="Máximo" Style="{StaticResource MenuButton}" Click="Maximo_Click"/>
                </WrapPanel>

                <Label Content="Recorridos" Foreground="White" Margin="0,10,0,5"/>
                <WrapPanel>
                    <Button Content="InOrden" Style="{StaticResource MenuButton}" Click="InOrden_Click"/>
                    <Button Content="PreOrden" Style="{StaticResource MenuButton}" Click="PreOrden_Click"/>
                    <Button Content="PostOrden" Style="{StaticResource MenuButton}" Click="PostOrden_Click"/>
                </WrapPanel>

                <Button Content="Reiniciar Árbol" Style="{StaticResource MenuButton}" Click="Reiniciar_Click" Width="200" HorizontalAlignment="Left" Margin="0,20,0,0"/>

                <TextBlock Text="Resultado:" Foreground="#C084FC" Margin="0,20,0,5" FontWeight="Bold"/>
                <TextBox x:Name="txtResultado" Background="#1A1A2E" Foreground="White" BorderBrush="#C084FC" 
                         MinHeight="60" FontSize="16" IsReadOnly="True" TextWrapping="Wrap" Padding="5"/>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="1" Background="#1A1A2E" BorderBrush="#C084FC" BorderThickness="2" CornerRadius="10" Margin="10">
            <Viewbox Stretch="Uniform">
                <Canvas x:Name="canvasArbol" Width="800" Height="600"/>
            </Viewbox>
        </Border>
    </Grid>
</UserControl>